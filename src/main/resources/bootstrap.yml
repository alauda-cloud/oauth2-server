spring:
  application:
      name: oauth2-server
  cloud:
    config:
      uri: http://config-server:8080
      name: ${spring.application.name}
      fail-fast: true
      retry:
        max-attempts: 10000
        max-interval: 1000